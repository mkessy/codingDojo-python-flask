CRUD

1. Create 
1. Read / Retrive
1. Update
1. Delete / Destroy


```sql
SELECT * FROM test_md.users;
-- C R U D
DELETE FROM test_md.users WHERE (id=82);

INSERT INTO test_md.users (first_name, last_name, email, pw) VALUES ("James", "Arbogast", "ja@email.com", "jamesjamesjames");

SELECT first_name as "First Name", last_name as "Last Name" FROM test_md.users;
SELECT first_name, last_name FROM test_md.users;
SELECT * FROM test_md.users;

UPDATE test_md.users SET first_name="Michael", last_name="Moulton", email="mm@email.com", pw="yourdad" WHERE (id = 1);
UPDATE test_md.users SET is_subscribed=1 WHERE (id = 80);
```

## Functions

```sql
-- CONCAT
SELECT CONCAT("Mr.", last_name) AS fullname FROM test_md.users;
SELECT CONCAT_WS(" ", "Mr.", last_name, first_name) AS fullname FROM test_md.users; -- with seperator

SELECT upper(first_name) FROM test_md.users;

```

